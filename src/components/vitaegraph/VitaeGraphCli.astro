<section class="page-section vg-cli-section" id="quick-start" aria-labelledby="quick-start-heading">
  <article>
    <p class="eyebrow">Local CLI</p>
    <h2 id="quick-start-heading">Markdown is canonical. Generated JSON is rebuildable.</h2>
    <p>
      The same <code>agentkit-seo</code> CLI initializes the workspace, validates links and IDs, then rebuilds local graph
      and search artifacts.
    </p>
  </article>
  <aside class="vg-command-panel">
    <pre class="code-block"><code>npx agentkit-seo graph init --root ~/.agentkit-seo/vitaegraph
npx agentkit-seo graph validate --root ~/.agentkit-seo/vitaegraph
npx agentkit-seo graph index --root ~/.agentkit-seo/vitaegraph</code></pre>
    <a class="text-link" href="/docs/cli/">Open the full CLI reference →</a>
  </aside>
</section>
